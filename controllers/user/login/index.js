import {
	redirectToSpotifyAuth,
	handleSpotifyCallback,
	fetchSpotifyUserData,
	isAuthenticated,
} from "./handler";

export default {
	redirectToSpotifyAuth,
	handleSpotifyCallback,
	fetchSpotifyUserData,
	isAuthenticated,
};